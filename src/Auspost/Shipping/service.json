{
    "name": "Shipping and Tracking Services",
    "apiVersion": "v1 Nov16",
    "description": "Australia Post Shipping and Tracking Services",
    "models": {},
    "includes": [],
    "operations": {
        "GetAccounts": {
            "httpMethod": "GET",
            "uri": "shipping/v1/accounts/{account_number}",
            "summary": "This interface retrieves information regarding the requestor's charge account and the postage products that the charge account is able to use.",
            "parameters": {
                "account_number": {
                    "location": "uri",
                    "description": "Account number used to retrieve accounts",
                    "required": true,
                    "type": "string"
                }
            }
        },
        "GetItemPrices": {
            "httpMethod": "POST",
            "uri": "shipping/v1/prices/items",
            "summary": "This interface retrieves valid postage products and pricing based upon the merchant location address, destination, and details of the parcel.",
            "parameters": {
                "body": {
                    "required": true,
                    "type": "string",
                    "location": "body"
                }
            }
        },
        "DeleteItem": {
            "httpMethod": "DELETE",
            "uri": "shipping/v1/shipments/{shipment_id}/items/{item_id}",
            "summary": "This service deletes an item in a shipment for shipments that have not been included in an order.",
            "parameters": {
                "shipment_id": {
                    "required": true,
                    "type": "string",
                    "location": "uri",
                    "description": "The shipmentId parameter refers to the unique Australia Post generated shipment_id"
                },
                "item_id": {
                    "required": true,
                    "type": "string",
                    "location": "uri",
                    "description": "The itemId parameter refers to the unique Australia Post generated item_id"
                }
            }
        },
        "GetShipments": {
            "httpMethod": "GET",
            "uri": "shipping/v1/shipments",
            "summary": "This service retrieves the information against shipments and the items contained within for shipments created using the Create Shipments interface.",
            "parameters": {
                "shipment_ids": {
                    "location": "query",
                    "description": "A comma separated list of shipment ids",
                    "required": false,
                    "type": "string"
                },
                "offset": {
                    "location": "query",
                    "description": "The offset parameter value specifies the numeric value of the first record to be returned in the result, beginning at 0. If you specify the offset parameter, you must also specify the number_of_shipments parameter.",
                    "required": false,
                    "type": "number"
                },
                "number_of_shipments": {
                    "location": "query",
                    "description": "The number_of_shipments parameter specifies the number of records to return in the result. If you specify the number_of_shipments parameter, you must also specify the offset parameter.",
                    "required": false,
                    "type": "number"
                },
                "status": {
                    "location": "query",
                    "description": "Setting the status parameter will result in the list returned only containing shipments that are at the given status. The parameter will accept one or more status values separated by a comma. E.g. status=CREATED,INITIATED",
                    "required": false,
                    "type": "string"
                },
                "despatch_date": {
                    "location": "query",
                    "description": "Setting the despatch_date parameter will result in the list returned only containing shipments that have one of the values as despatch_date. The parameter will accept one or more despatch_date values in the format of yyyy-MM-dd separated by a comma. E.g. despatch_date=2016-04-04,2015-12-31",
                    "required": false,
                    "type": "string"
                },
                "sender_reference": {
                    "location": "query",
                    "description": "Setting the sender_reference parameter will result in the list returned only containing shipments that have one of the values as sender_reference. The parameter will accept one or more sender_reference values separated by a comma. E.g. sender_reference=MYREF-12345",
                    "required": false,
                    "type": "string"
                }
            }
        },
        "CreateShipments": {
            "httpMethod": "POST",
            "uri": "shipping/v1/shipments",
            "summary": "This interface creates a shipment with items and returns a summary of the pricing for the items.",
            "parameters": {
                "body": {
                    "required": true,
                    "type": "string",
                    "location": "body"
                }
            }
        },
        "UpdateShipment": {
            "httpMethod": "PUT",
            "uri": "shipping/v1/shipments/{shipment_id}",
            "summary": "This service updates an existing shipment that has previously been created using the Create Shipment interface.",
            "parameters": {
                "shipment_id": {
                    "required": true,
                    "type": "string",
                    "location": "uri"
                },
                "body": {
                    "required": true,
                    "type": "string",
                    "location": "body"
                }
            }
        },
        "DeleteShipment": {
            "httpMethod": "DELETE",
            "uri": "shipping/v1/shipments/{shipment_id}",
            "summary": " This service deletes one or more shipments that have not been included in an order.",
            "parameters": {
                "shipment_id": {
                    "required": false,
                    "type": "string",
                    "description": "The shipment id you wish to delete",
                    "location": "uri"
                },
                "shipment_ids": {
                    "required": false,
                    "type": "string",
                    "description": "A comma separated list of shipment ids",
                    "location": "uri"
                }
            }
        },
        "CreateLabels": {
            "httpMethod": "POST",
            "uri": "shipping/v1/labels",
            "summary": "This service initiates the generation of labels for the requested shipments that have been previously created using the Create Shipments service.",
            "parameters": {
                "preferences": {

                },
                "shipments": {
                }
            }
        },
        "GetLabel": {
            "httpMethod": "GET",
            "uri": "shipping/v1/labels/{request_id}",
            "summary": "This service returns information on a label request created using the Create Labels service.",
            "parameters": {
                "request_id": {
                    "location": "uri",
                    "description": "the label to retrieve by id",
                    "required": true,
                    "type": "string"
                }
            }
        },
        "ValidateSuburb": {
            "httpMethod": "GET",
            "uri": "shipping/v1/address",
            "summary": "This service validates a suburb, state, and postcode combination for an Australian postal address and returns the valid suburbs for the given state and postcode.",
            "parameters": {
                "suburb": {
                    "required": true,
                    "type": "string",
                    "location": "query"
                },
                "state": {
                    "required": true,
                    "type": "string",
                    "location": "query"
                },
                "postcode": {
                    "required": true,
                    "type": "integer",
                    "location": "query"
                }
            }
        },
        "TrackItems": {
            "httpMethod": "GET",
            "uri": "shipping/v1/track",
            "parameters": {
                "tracking_ids": {
                    "location": "query",
                    "description": "A comma separated list of tracking numbers",
                    "required": true,
                    "type": "string"

                }
            }
        },
        "CreatePayment": {
            "httpMethod": "POST",
            "uri": "shipping/v1/payments",
            "parameters": {
                "order_id": {
                    "required": true,
                    "type": "string",
                    "location": "json"
                },
                "payment_method": {
                    "required": true,
                    "type": "string",
                    "location": "json"
                },
                "payment_reference": {
                    "required": true,
                    "type": ["integer", "string"],
                    "location": "json"
                },
                "attributes": {
                    "required": true,
                    "type": "number",
                    "location": "json",
                    "parameters": {
                        "account_number": {
                            "required": true,
                            "type": ["integer", "string"],
                            "location": "json",
                            "sentAs": "account-number"
                        }
                    }
                }
            }
        }
    }
}
